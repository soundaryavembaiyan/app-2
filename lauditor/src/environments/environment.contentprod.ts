// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  apiUrl: 'https://api.digicoffer.com/professional',
  emailApi:"https://mailapi.digicoffer.com/api/v1",
  paymentgatway:"https://payment.digicoffer.com/",
  doc2pdf:"https://prod.utils.doc2pdf.digicoffer.com/api/v1/doc2pdf",
  DOC2FILE :'https://prod.utils.doc2pdf.digicoffer.com/api/v1/docfile2pdf',
  chatops:' https://prod.utils.chat.digicoffer.com',
  lateXAPI: ' https://prod.utils.latex.digicoffer.com',
  AVChat:' https://avchat.digicoffer.com/',
  xmppDomain: 'chat.vitacape.com',
  product: 'content',
  production: true
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
